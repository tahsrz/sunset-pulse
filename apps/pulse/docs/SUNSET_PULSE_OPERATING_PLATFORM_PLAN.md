# Sunset Pulse operating platform — three-phase implementation plan

Revised September 18, 2026 from Taz's five architecture changes. This is the active plan.
Implement sequentially, without subagents. Read this file and only the source files needed for the current action.
The [previous 18-packet specification](archive/SUNSET_PULSE_OPERATING_PLATFORM_PLAN_2026-09-18.md) is historical evidence, not a second implementation backlog.

Execution entry: [current ledger](#current-ledger-and-next-session) → [September 21–25 handoff](PLATFORM_WEEK_2026-09-21.md). The code sequences below describe the architecture; they are not all unfinished tasks. The requested `SUNSET_PULSE_OPERATING_PLATFORM_PLAN_2.md` does not exist in this checkout; this file is the canonical replacement, not a second plan to create.

Vision: people and Jamie organize business work together. Installed apps describe inputs, workflow graphs and required tools as data. One scheduler runs the work, waits for people, preserves evidence and reports outcomes.

## Architecture decisions

| Requested change | Implementation decision |
| --- | --- |
| JSONB state machines | Store the pinned versioned graph, definition hash, cursor, generation and bounded answers/results in one `platform_runs` row. Reuse `workflow_jobs`, `workflow_results` and `workflow_event_contracts`. No new events, outbox, consumer-receipt or run-step tables for same-Postgres transitions. |
| One checkpoint primitive | One `platform_checkpoints` table with `question`, `approval`, `effect_gate`, one response service/API and one future inbox card. Preserve type-specific response validation, roles and exact targets. |
| Declarative apps | Persist validated JSON manifests and installation settings. Zod validates the JSON format; Zod instances/functions are never stored. Real-estate and content workflows become JSON fixtures/manifests consumed by the same interpreter and form renderer. |
| MCP/OpenAPI capabilities | Use shared protocol clients and JSON Schema input/output contracts. Tools are identified by connection ID + tool/operation name + pinned schema hash. Keep credentials, entitlement checks, effect classification and host access in server policy. |
| Three macro phases | Core Platform → Declarative Engine → Control Layer. Track completed vertical slices and actual evidence, not 165 repeatedly loaded instruction anchors. |
| Spatial command center proposal | Add a workspace canvas and strict command shell as a Control Layer surface over existing read models and authenticated APIs. Persist only bounded layout preferences; never duplicate run/checkpoint state or call worker RPCs directly from the browser. |

An effect gate records a decision; it is not a delivery receipt. Reuse existing email/CMS/payment receipts and their exact-version checks. New connectors must persist operation identity and reconcile uncertain provider outcomes before replay. MCP defines tool schemas/calls, not Sunset Pulse authorization, transactionality or duplicate-delivery guarantees. Its annotations are not authoritative permission policy. See the [MCP tools specification](https://modelcontextprotocol.io/specification/2025-06-18/server/tools).

JSONB reduces schema and join overhead; it does not eliminate concurrency control. Serialize transitions on one run row, compare revisions/generations, fence worker writes with live leases, and bound graph/state size. Large artifacts remain in existing artifact/domain stores. Start with one active node per run; parallel branch execution is a later measured need.

Postgres run/checkpoint changes enqueue directly into the existing queue inside the same transaction. Mongo/storage handoffs still use their existing durable intents and reconciliation. Removing a Postgres outbox does not create a cross-database transaction.

## Existing foundation and known limits

- Reuse identity, memberships, audit and scope links from `20260917010000_platform_workspaces.sql` through `20260918010000_platform_sprint_schedule_backlog_scope.sql`.
- Reuse `requireSignedInUser`, `workspaceAccess.server.ts`, `durableScheduler.server.ts`, the event contract registry and fenced job completion.
- Preserve property truth, email review/recipient rules, Vibe exact revision review, scan privacy and existing billing ledgers.
- Workspaces and scope adapters have local unit/build and disposable Postgres evidence. Local Supabase browser/password-session acceptance now passes for workspace creation, run start/answer/cancel, cursor pages and foreign-user denial. This is not production or all-domain RLS acceptance; the earlier production dry-run failed with an invalid API key and performed no writes.
- Existing owner compatibility paths, team resource mapping, manual sprint edits/completion and schedule controls still need review. Schedule/backlog scope wrappers are code present, not proof of complete team isolation. Check mapping transfers, requester-versus-resource-owner selection, archived workspace denial and scheduler input selection before enabling team workflows.
- Existing scans are a separate partial bridge; real reconstruction remains unavailable.
- No production migration, backfill, deployment, outbound send or paid-provider activation is part of these local fixtures.

## Phase 1 — Core Platform

Goal: a validated JSON workflow persists, waits for a person, accepts one answer/decision, resumes after restart and reaches a terminal state through the existing scheduler.

### Implemented backend contract

The following backend sequence is implemented and tested locally. Do not rebuild it. Team-domain integration and end-user UI are separate remaining work.

1. `lib/platform/contracts/run.ts`: strict schema version 1 graph; keys/versions; unique node IDs; entry and next references; 1–64 nodes; reject missing/unreachable nodes and cycles.
2. Define `checkpoint` and `complete` nodes first. A question uses a supported JSON Schema scalar (`string`, bounded string enum, `number`, `boolean`). Approvals/effect gates pin action, resource ID/type, revision and content hash; audience hash where applicable. Rejection cancels the run.
3. `supabase/migrations/20260918020000_platform_json_runs_checkpoints.sql`: create only `platform_runs` and `platform_checkpoints`. Store graph/state JSONB, immutable answer attribution, request/submission keys and revisions. Composite checkpoint/run workspace FK prevents mismatched ownership. Index workspace inbox/status/time.
4. Restrict table writes to the security-definer operations; authenticated users get scoped read access only. Require active workspace and membership. Use a shared workspace/member lock before run/checkpoint mutation so revocation/archive cannot race an accepted transition.
5. `platform_start_run`: validate graph and requesting role; replay identical request key/content; reject changed content; insert run + generation-1 queue event + audit atomically.
6. `platform_tick_run`: load the persisted job, verify key/version/owner/workspace/generation and live lease; lock run; open its checkpoint or finish it; save `workflow_results` receipt and job completion in the same transaction. Expired leases roll back state and checkpoint writes.
7. Human waits complete the scheduler tick with the business run still `waiting`. They neither hold a connection nor consume polling/retry budgets while waiting for a person.
8. `platform_respond_checkpoint`: lock run then checkpoint; check checkpoint-specific role, pending state, expected revision and typed value. Preserve immutable response/submission key/actor. Advance JSON cursor/generation and enqueue the next event in the same transaction. Same submission replays; a different answer conflicts.
9. `platform_cancel_run`: revision-check cancellation and cancel pending checkpoints. Later ticks record a no-op receipt; they cannot reopen a terminal run. Revoked run requesters block future work.
10. `lib/platform/workflows/runStore.server.ts`: validated start/list/respond/cancel adapters; `runHandler.server.ts`: scheduler handler. Add a `committed` result variant so the scheduler does not write a second receipt after the atomic RPC.
11. `app/api/workspaces/[workspaceId]/runs/route.ts`: GET bounded runs, POST start, PATCH cancel. `checkpoints/route.ts`: GET pending checkpoints, POST typed response. Shared HTTP adapter enforces session, workspace, streamed body-size limit, same-origin browser writes, private/no-store responses and safe conflict errors.
12. Register `platform_run@1` in the existing database event contracts **disabled by default**. Deploy a compatible worker before enabling admissions; enable only in disposable tests during this slice.
13. Exercise duplicate starts, changed graphs, malformed/cyclic graphs, competing responses, wrong types, role denial/revocation, cancellation and stale leases using real disposable Postgres. Verify graph/API contracts and legacy scheduler behavior with focused tests.

First-version scope: scalar questions, approvals, effect gates and completion. No connector invocation, model call, domain fact mutation or external action is implied by a checkpoint response. The exact target snapshot must be revalidated by the future effect adapter at dispatch.

### Follow-on Core actions

- Next: finish workspace-aware planner input selection, then validate the new atomic persistence and legacy mutation compatibility; use W1–W2 in the [weekly handoff](PLATFORM_WEEK_2026-09-21.md). September 22 adds mapped read IDs, a legacy owner-fallback guard and a scoped manual proposal RPC; retain fail-closed team admission until Docker/SQL concurrency acceptance passes.
- Then: wire a property assignment into a manifest-launched run only when its property and task revision are explicitly mapped (W3).
- Implemented: bounded workspace/collection-bound cursor pages ordered by timestamp + ID; admin recovery requires restored requester authority; supersession creates a new pinned run and retains prior answers/actor attribution. Lists now return `{ items, nextCursor }`.
- Completed locally: real Supabase password/cookie browser sessions prove start, answer, cancel, cursor pages and foreign-user denial. The local runner leases only its own fixture jobs; normal claim contention is covered separately by disposable scheduler acceptance. Production and future inbox UI acceptance remain separate gates.
- Add richer question fields and conditional nodes only with matching schema/interpreter tests; unsupported graph versions fail explicitly.

## Phase 2 — Declarative Engine

Goal: real-estate readiness and client-content review are two data-defined apps using the same interpreter and tooling.

### Code sequence

Items 1–3 are implemented for inert intake-only manifests. Items 4–16 are not implied by installation; W3 adds authorized launch, and W4 may add the human-only form/inbox surface. Tool execution remains gated behind policy, receipts and cost controls.

September 22–23 safety slice: `lib/platform/contracts/capabilityPolicy.ts` defines bounded connection/tool/schema-hash metadata and immutable effect-receipt fields. `20260922110000_platform_capability_receipts.sql` persists revisioned, allowlisted read/prepare policies and append-only operation receipts with workspace/run linkage, hashes, terminal-resolution checks and service-only writes. `20260922120000_platform_capability_admission.sql` adds revisioned workspace quotas and an idempotent admission reservation that checks the pinned policy, operation identity and schema hashes before reserving step/cost budget. Follow-up migration `20260923160000_platform_quota_budget_settlement.sql` adds per-run token/cost/time ceilings, actual usage settlement, bounded audited expiry reconciliation, authenticated owner/admin revision-checked quota APIs, and release of outstanding reservations inside run cancellation. `connector.ts` plus `20260922130000_platform_connector_snapshots.sql` define HTTPS-only reviewed connector metadata and revisioned input/output schema snapshots using the bounded object-schema validator; no credentials or protocol calls are stored/executed. `capabilityAdmission.server.ts` resolves the reviewed connector and exact input snapshot, validates the payload, then consumes the existing reservation RPC without dispatching a provider. `protocolGateway.ts` defines bounded MCP/OpenAPI request/response envelopes; `protocolGateway.server.ts` can prepare a fixture request only after admission, while `invokeCapability` is explicitly disabled. The inert policy rejects external effects, duplicate capability identities, unallowlisted connections and executable fields; no provider is dispatched. `market-digest.v1.json` proves a third app can validate through the same manifest schema and scalar form contract without a vertical TypeScript package. The bounded condition contract is admitted end-to-end: TypeScript graph validation, forward SQL validation, deterministic `answers.*` evaluation, branch reachability checks, queued generation transitions and app-manifest admission all agree. Conditions remain non-executable predicates; MCP/OpenAPI dispatch and provider effects remain disabled.

1. `lib/platform/contracts/appManifest.ts`: parse JSON fields `schemaVersion`, `key`, `version`, `title`, `inputSchema`, `workflows`, `capabilities`, `artifactSchemas`, `settingsSchema`. Reject executable strings, unknown schema features and dynamic imports.
2. Add `platform_app_installs` holding pinned manifest JSON/hash, workspace settings, status and revision. A run copies its selected definition/version at admission; upgrading an install cannot rewrite active runs.
3. Store `real-estate-readiness.v1.json` and `client-content-review.v1.json` as reviewed import fixtures. Installation persists the validated JSON. New apps using supported fields/tools need no per-vertical TypeScript package.
4. Build one manifest form renderer supporting the documented JSON Schema subset, with field labels, descriptions and preserved drafts. Unsupported widgets/schemas return a useful validation error, not guessed controls.
5. `lib/platform/contracts/condition.ts` and `supabase/migrations/20260922100000_platform_condition_nodes.sql`: admit bounded `exists`, `equals`, `all` and `any` predicates with `answers.*` paths only; validate both branches, reject cycles/convergence/unreachable nodes, evaluate deterministically in the worker and enqueue exactly one next generation. Keep capability nodes and provider execution unavailable.
6. Add a policy admission service around `platform_capability_policies` and `platform_effect_receipts`; require a pinned read/prepare declaration, schema hashes, workspace connection allowlist and an operation identity before any future gateway call. Keep `lib/platform/capabilities/protocolGateway.server.ts` disabled until provider-specific quotas, overrun handling, and activation review are complete.
7. `lib/platform/capabilities/protocolGateway.server.ts`: one dispatch function `invoke({ connectionId, tool, schemaHash, arguments, operationId })`; protocol adapters for MCP `tools/list`/`tools/call` and OpenAPI `operationId`. Prefer the maintained SDK after checking the installed/runtime-compatible version.
8. `lib/platform/contracts/connector.ts` and `supabase/migrations/20260922130000_platform_connector_snapshots.sql`: persist reviewed HTTPS connector definitions and immutable input/output schema snapshots. Credentials remain references to workspace secrets; a manifest cannot add a host, executable command, credential or permission. Missing connections and changed schemas block affected nodes visibly.
9. Validate tool input and output against pinned JSON Schema. Enforce timeouts, response size, server-side action policy and quota before execution. Model-supplied JSON and MCP annotations cannot grant new scope.
10. Classify tools as read, prepare or external effect under server policy. Internal domain operations may use thin shared handlers because existing SQL/CMS/email invariants still need enforcement; avoid a class hierarchy or wrapper per workflow step.
11. Persist exact output versions/provenance in existing domain stores and put references/hashes in run state. Run snapshots and generic answers do not silently update authoritative property facts.
12. For external effects, persist stable operation ID, payload/target hash, gate reference and receipt reference before submission. Use provider idempotency, or mark uncertain acceptance `unknown` and reconcile. Reuse email/CMS/billing ledgers; the shared effect receipt store now covers connector evidence.
13. Snapshot-based approval and effect gates share the checkpoint API, while the executor rechecks current authority, source revision, audience, consent, entitlement and budget immediately before acting.
14. Prove both manifests on deterministic connector fixtures first. Then enable explicitly configured providers with cost reservations and recorded outputs. Unsupported capabilities stay unavailable.
15. Property flow: existing shortlist → reviewed sprint assignment → question → sourced brief/draft → approval → separate email effect gate → authoritative delivery receipt.
16. Content flow: manifest input form → missing-information question → draft → approval → effect gate → exact Vibe revision/site application. No scheduler or inbox fork.
17. Persist app install/upgrade/uninstall audit; uninstall stops new admissions and leaves existing evidence intact.

Exit: adding a third app using existing capabilities is a new validated manifest plus settings, not another execution service. New vendor protocols or domain invariants can still require a shared adapter.

## Phase 3 — Control Layer

Goal: people operate the engine from one exception inbox, with observable cost, limits and recoverable failures.

### Code sequence

1. `components/platform/CheckpointCard.tsx`: one discriminated card for questions, approvals and effect gates. Show exact targets/consequences, role limits, validation errors and recoverable drafts. Bulk actions remain individually bound to displayed versions.
2. `app/workspaces/[workspaceId]/inbox/page.tsx`: one queue of pending checkpoints and blocked/stale/unknown runs. Cursor pagination, summary counts and bounded refresh; update route catalog and root README together.
3. Run details show graph progress, questions/decisions, output references and scheduler/effect receipts. Use safe metadata, never raw secrets or cross-workspace traces.
4. Connect Jamie to the same read/response/start services. Chat can propose actions; it does not create a second question/approval store or infer send permission.
5. Extend existing `platform_audit_events` for state transitions, submissions, policies and installs. Start/answer/cancel/worker audit exists in the core slice; do not postpone basic attribution to this phase.
6. Implemented locally: transactional workspace concurrency and estimated-cost exposure, per-run step/token/estimated-cost/time limits, idempotent actual-cost/token settlement, audited bounded expiry reconciliation, owner/admin revision-checked budget API and reservation release in run cancellation. The workspace inbox now includes a compact budget editor and one-shot reconciliation action; the worker rechecks owner/admin authority and processes at most 100 reservations through a disabled-by-default durable-scheduler event. Actual cost/token estimate overruns now create immutable workspace/run-scoped breach evidence plus safe audit events; a database insert fence rejects every later reservation for that run while leaving unrelated runs unaffected. Quotas remain transactional, not UI counters; external dispatch remains disabled.
7. Measure scheduler fairness and claim latency under mixed legacy/platform load. Add workload routing/deadline controls to the existing worker, not a second queue.
8. Add connector health/schema-change alerts, unknown-effect recovery, stale input replacement and manual retry that retains operation identity.
9. Add team invitations, scoped external reviewers, access revocation, retention and restore drills; shared checkpoint UI uses the same resource policies.
10. Pilot both apps with real permission and economic evidence: accepted outputs, response time, inquiries/bookings/publications, provider costs and support overhead. Pricing follows observed results and explicit business decisions.

### Proposed follow-on: spatial command center and command shell

This is a product/UI proposal derived from the supplied Gemini design, not work already implemented and not a replacement scheduler. Complete the cost-overrun/provider-limit gate before starting this follow-on. The canvas is a composable view of the existing platform; `platform_runs` and `platform_checkpoints` remain the operational source of truth. They are revisioned mutable operational records with immutable response/audit evidence—not wholly immutable ledgers. Treat the Gemini phrase “standard input for atomic RPCs” as an API-client metaphor: browser input must use authenticated HTTP routes, and only existing trusted server services may call security-definer RPCs.

Implementation sequence, after quota operations/recovery and core inbox/run-detail gates:

1. `lib/platform/contracts/canvasLayout.ts`: define strict version-1 Zod layout JSON: workspace ID, bounded viewport coordinates/zoom, at most 32 windows, UUID window IDs, allowlisted window kinds (`checkpoint_inbox`, `run_graph`, `artifact_viewer`, `system_monitor`, later `terminal` only if a real safe terminal resource exists), bounded x/y/width/height and z-order. Use a discriminated target schema; reject unknown fields, cross-workspace identifiers, non-finite geometry, duplicate window IDs, and arbitrary component names. Do not persist DOM state or workflow data in the layout blob.
2. Migration `platform_user_layouts`: one row per `(workspace_id,user_id)`, schema version, JSONB layout, revision, timestamps and workspace/user foreign keys. Enable RLS; members can read their own row, but writes go through a revision-checked owner-scoped RPC/service that rechecks active membership. Bound serialized JSON size and window count in both Zod and SQL. Keep layout private to its user; do not share another member's canvas implicitly.
3. Add `GET/PATCH /api/workspaces/[workspaceId]/layout`: use `requireSignedInUser`, workspace access, same-origin protection for writes, private/no-store caching, request byte limits, and expected-revision conflict responses. Persist only on explicit save, drag/resize completion, close, pin, or viewport-settle—not on each animation frame. Default layout is generated in the client when no row exists; opening the canvas must not create run/checkpoint records.
4. `components/platform/CanvasOS.tsx`, `CoordinatePlane.tsx` and `WindowFrame.tsx`: compose existing `PlatformInbox`, `RunDetail`, and bounded artifact/health views as allowlisted window bodies. Keep pointer-frequency pan/drag/zoom state in a local React store/context; hydrate/save through the layout API only at interaction boundaries. Use accessible focus order, keyboard move/resize alternatives, reduced-motion support and a conventional list/navigation fallback. Start with existing React/CSS primitives; adopt React Flow or React Three Fiber only after a prototype demonstrates a concrete benefit and performance/accessibility review.
5. `components/platform/CommandPalette.tsx` and `lib/platform/commands/grammar.ts`: parse a deliberately small, typed grammar. `/start <manifest-key>@<version>` validates against installed manifests and calls the existing app-launch route; `/cancel <run-id> --reason=<bounded-code>` uses the existing run cancellation route; `/ps` uses the bounded workspace runs read API. Return a preview/result card and map successful start responses to a canvas window only after server confirmation. Never expose `platform_tick_run`, `platform_admit_capability_operation`, SQL, credentials, arbitrary URLs, or operating-system commands to the client grammar.
6. `:focus inbox`, `:focus run <id>`, `:close <window-id>` and `:reset-layout` are presentation commands only. Validate target visibility through the existing workspace-scoped read model before focusing; clear/reset only the requesting user's layout. No `:close all` may cancel jobs or alter business evidence.
7. Defer `>` piping until there are typed, version-pinned artifact references, explicit producer/consumer schema compatibility, authorization rechecks and an approval boundary. A first release must not chain outputs directly into approvals, capability calls, email, CMS publication, or other effects. Any future pipe is a proposal to existing app/run APIs, not a new executor.
8. Jamie's `? <natural-language request>` path is suggest-only: send text to the existing Jamie read/proposal service, render a structured proposal card, and require a person to confirm it through the existing APIs/checkpoints. Jamie cannot dispatch parsed commands, mint approvals, alter layout targets across workspaces, or infer send/publication permission. Keep this integration disabled until its existing service and access model are verified.
9. Tests: unit-test grammar and layout schemas (unknown commands/keys, out-of-bounds values, duplicate windows, target/workspace mismatch); API-test own-layout RLS, foreign-user denial, stale revision and body limits; browser-test save/restore, reload recovery, pointer interactions, keyboard navigation, low-motion/list fallback, command preview, and that cancel/start affect exactly the existing API resource. Add performance checks before enabling dozens of windows or realtime fan-out.
10. Release gates: merge only after the existing inbox/run screens remain available as a fallback, layout state is isolated per member, browser writes cannot invoke service-only RPCs, and all command mutations retain current CSRF/origin/auth/idempotency behavior. Do not enable piping, provider calls, production activation, email, or publication as part of the canvas launch.

Initial outcome should be a practical command center—not a literal infinite desktop: users can arrange inbox, selected run, artifacts and connector health; reload to restore their own view; start/cancel through approved application routes; and ask Jamie for suggestions that a human explicitly accepts.

Exit: two apps can be operated and recovered through one UI with measured costs and no private-data crossover.

## Verification and release

Use the exact commands and scope distinctions in the [September 18 verification record](PLATFORM_VERIFICATION_2026-09-18.md) and [Docker/Auth runbook](../../../infra/local/README.md). Add focused tests for each new slice; existing passes do not validate later changes.

New event admissions remain disabled until the matching worker version and release gates are satisfied. Production migrations, backfills, promotion, outbound sends and paid-provider activation require a separate explicit operational decision. No merge/deploy is authorized by this plan.

## Current ledger and next session

| Phase | Current state | Evidence / remaining gate |
| --- | --- | --- |
| Core Platform | JSON run/checkpoint backend, pagination, recovery, supersession, scope fencing and workspace-scoped planner persistence implemented locally. | Local real-Postgres and real Supabase/browser acceptance pass, including revoked/archived/foreign denial. Production admission remains disabled. |
| Declarative Engine | Manifest contract, revision-checked install API, three reviewed intake fixtures, server-authoritative app launch, bounded condition branching, revisioned capability policies, append-only effect receipts, transactional workspace/run budgets, provider/adapter ceilings, and a bounded provider-exception inbox view implemented locally. | Provider reviews and quota revisions are workspace-scoped and audited; reservations pin the reviewed contract. Over-budget actual settlement records an immutable UTC-day breach and fences subsequent admission; review revocation fences new work but preserves settlement for in-flight reservations. Exception pages are independently keyset-paginated and workspace/collection-fenced, with safe links to affected run/reservation evidence. Vibe/content authority remains unavailable. No gateway dispatch, live capability execution or external effects; manifests still require empty capabilities. |
| Control Layer | Shared checkpoint inbox, ManifestForm, CheckpointCard and run details implemented locally with route inventory updates. | Local rendered browser acceptance and fresh PR CI pass. Quotas, connector operations, Jamie integration and a two-app operational pilot remain later gates. |

Baseline implementation: `3af65f44` on [PR #79](https://github.com/tahsrz/sunset-pulse/pull/79). Fresh CI at that head passed lint, test, scheduler-db, docker-acceptance, jamie-e2e and Vercel; Supabase Preview was skipped. Exact evidence, migration scope and remaining gaps live in the [dated record](PLATFORM_VERIFICATION_2026-09-18.md) and [weekly handoff](PLATFORM_WEEK_2026-09-21.md), not in repeated completion logs here.

**Completed safety slice:** deterministic MCP/OpenAPI fixture responses are validated against the pinned output snapshot, and effect-gate/checkpoint receipt transitions are proven without network calls. Keep real provider calls, credentials and production activation disabled.

**Completed safety slice:** fixture response hashes/provenance are linked to pinned connector snapshots, and changed snapshot hashes block a live run through the existing recovery boundary.

**Completed safety slice:** reviewed connector health evidence now persists bounded status/check timestamps and snapshot hashes, and the shared checkpoint inbox surfaces unavailable, schema-drifted and stale connections without dispatching providers. The acceptance fixture proves idempotent upsert and service-only writes; the health detail allowlist excludes credential, executable and arbitrary dynamic fields.

**Completed safety slice:** a `connector_health_check` event handler now uses the existing durable scheduler and records only bounded fixture evidence through `platform_record_connector_health`. Its event contract is disabled by default; it never calls a provider or reads credentials. Unit coverage proves reviewed snapshots become `healthy` and disabled/missing-snapshot connections become `unavailable`.

**Completed safety slice:** workspace operators now have an authenticated, workspace-scoped connector health-check event boundary with explicit connector IDs and replay keys. The contract remains disabled by default; the route returns a safe unavailable response until an operator enables the contract, and the acceptance fixture proves duplicate event replay plus lease recovery through the existing scheduler. No provider probe, credential read or second queue was added.

**Completed safety slice:** the shared inbox now derives bounded connector freshness states (`fresh`, `due`, `queued`, `running`, `overdue`) from the latest health evidence and existing scheduler jobs. Operators can schedule a fresh fixture check with a new replay key; prior health evidence remains immutable, and no provider is dispatched.

**Completed safety slice:** the shared inbox now exposes workspace-scoped connector-health summary counts and an independent bounded keyset page, so health history can be reviewed without changing checkpoint pagination. The database summary is service-role-only, health cursors carry workspace/collection fences, and acceptance coverage proves a foreign workspace cannot read the summary. The combined read model still uses persisted evidence and the existing scheduler job table; live provider probes, credentials and production activation remain disabled.

**Completed safety slice:** connector health transitions now append immutable bounded history, and scheduler job UUIDs produce idempotent health-result receipts through a service-only RPC. The existing checkpoints read model exposes a separate bounded health-history cursor, while acceptance proves replay safety, receipt target validation, and foreign-workspace denial. Keep live provider probes, credentials and production activation disabled.

**Completed safety slice:** health-result receipts are now joined into the workspace health read model, and first-write receipts emit service-attributed `platform_audit_events` with safe receipt, operation and result-hash metadata. Replay remains idempotent, and the acceptance fixture verifies the audit link plus foreign-workspace denial.

**Completed safety slice:** connector-health job insertion now emits one audited scheduling event at the existing scheduler boundary, while event replay remains insert-idempotent. Acceptance also runs concurrent receipt writes against one operation ID and proves one receipt survives with the immutable history chain intact and no foreign-workspace access.

**Completed safety slice:** the shared inbox read model now exposes a separately bounded, workspace-fenced audit page for connector scheduling and result events. It filters to the reviewed action allowlist and returns only safe metadata, while audit cursor collection fences prevent cross-workspace or cross-collection reuse.

## Work completed so far — connector-health operating slice

This is the current implementation summary to use as research context before the next sprint:

1. The connector-health contract is bounded to reviewed statuses (`healthy`, `unavailable`, `schema_drift`, `stale`) and an explicit safe-detail allowlist. Credentials, executable content, provider response bodies and dynamic fields are rejected.
2. Health evidence is stored workspace-scoped with connector and schema-snapshot references. Writes use a service-only RPC; authenticated users can read only through existing workspace policies.
3. A `connector_health_check` worker was added to the existing durable scheduler. It only evaluates fixture/pinned-snapshot evidence and never contacts a provider or reads credentials. Its event contract remains disabled by default.
4. Workspace operators can request a health check through an authenticated, cross-origin-protected route with an explicit connector ID and replay key. Disabled scheduling returns a safe unavailable response.
5. The shared inbox derives `fresh`, `due`, `queued`, `running` and `overdue` states from persisted health evidence plus existing scheduler jobs. Retry scheduling does not replace prior evidence.
6. Health summary counts and health records have independent bounded keyset cursors. Cursors carry workspace and collection identity, preventing reuse across workspaces or read models.
7. Every health transition appends bounded immutable history. Scheduler job UUIDs serve as operation identity for idempotent result receipts; duplicate receipt writes return the original receipt.
8. Receipt records are joined into the workspace health read model and emit service-attributed audit events with only receipt ID, operation ID, health ID, status and result hash metadata.
9. Health scheduling is audited at the existing workflow-job insert boundary. Scheduler replay does not create a second scheduling audit event.
10. The inbox read model exposes a separate bounded audit page containing only `connector.health.scheduled` and `connector.health.receipt_recorded` actions. Audit cursors are independently workspace/collection fenced.
11. Acceptance coverage proves workspace creation and membership, scheduler replay, lease recovery, authenticated RLS isolation, connector health upsert/history, disabled admission, receipt replay, concurrent receipt writes, audit linkage and foreign-workspace denial.
12. The focused unit suites and production builds have passed for each completed slice. The Docker/Postgres scheduler acceptance has passed after the history, receipt and scheduling-audit migrations were added. The known build diagnostic is the existing nonfatal dynamic usage message from `/api/kepler/listings`.

Implementation commits for this slice, newest first: `a1c55a87` (bounded audit history), `63c9fef6` (scheduling audit), `349c7ef3` (receipt audit/read model), `b194f0dd` (history and receipts), `2b7b0234` (health pagination), `48a31afa` (freshness states), `40bb9eb9` (operator scheduling) and `a9663e74` (disabled scheduler boundary). No production migration, provider activation, push or deployment was performed.

**Completed safety slice:** connector-health evidence has no automatic purge while owners select retention periods by data class. The local runbook preserves the current health projection, diagnostic history, operation-deduplication receipts and actor/result audit evidence; any future receipt pruning must retain operation tombstones, and restores must never replay old jobs/effects. Disposable Postgres acceptance now snapshots, removes and restores one workspace's history, receipts and audit events with original IDs, checks row counts and unique operation identities, and verifies a foreign workspace remains empty. This is an isolated evidence restore rehearsal, not production backup/PITR acceptance.

**Completed safety slice: transactional quotas and cost settlement** — migration `20260923160000_platform_quota_budget_settlement.sql` adds configurable per-run token, estimated-cost and elapsed-time ceilings alongside existing workspace concurrency/estimated exposure and per-run step limits. Admission reserves bounded estimates transactionally, counts consumed actual usage toward run budgets, expires timed reservations without deleting operation identities, and rechecks identity after the serialized quota lock to make concurrent replays idempotent. `platform_settle_capability_operation` records consumed actual tokens/cost or releases/expires a reservation exactly once; same-value replay returns the original row, conflicting replay is rejected, and the transition emits safe service-attributed audit evidence. The TypeScript admission contract passes bounded estimated tokens. The inbox quota editor is owner/admin-only and revision-checked. Migration `20260924100000_platform_quota_reconciliation_event.sql` registers a disabled-by-default event; the API schedules one batch, the worker rechecks authority, and the service-only RPC caps each batch at 100 while recording audit evidence. Docker acceptance covers actual-vs-estimated budgets, settlement replay/conflict, cancellation release, batch expiry, event disable-by-default, event replay/lease recovery and a concurrent admission race; focused unit tests cover API authorization and worker bounds. These are accounting controls only: provider dispatch and production activation remain disabled.

**Completed action: provider adapter reviews, immutable pins, and transactional provider quotas.** `providerAdapter.ts` validates versioned provider/adapter identity, explicit USD pricing dimensions and billable bounds, idempotency, and unknown-outcome recovery metadata using conservative integer micro-dollar arithmetic. The owner/admin review route `POST /api/workspaces/{workspaceId}/connectors/{connectionId}/provider-adapter` writes through `platform_register_provider_adapter_review`; migration `20260923180000_platform_provider_adapter_reviews.sql` validates/hashes each immutable adapter version, pins its review/hash to the connector, and clears the connector pin when endpoint/protocol/auth changes. Migration `20260923190000_platform_provider_quotas.sql` adds revisioned workspace/provider/adapter ceilings, owner/admin save and revoke RPCs, and immutable UTC-day breach evidence. `POST /api/workspaces/{workspaceId}/quotas/providers` manages concurrency, reserved-cost exposure and max actual daily cost; `POST /api/workspaces/{workspaceId}/provider-adapters/{reviewId}/revoke` revokes an unsafe/stale review. The reservation trigger locks the provider quota row, requires a current reviewed pin and sufficient reserve for the reviewed maximum operation cost, and serializes provider concurrency/exposure/projected-day admission. Settlement accounts consumed actual cost; crossing the UTC-day limit records one breach plus safe audit evidence and blocks further reservations for that adapter until the next UTC day. Revocation denies new reservations without deleting review history or preventing settlement of already-pinned work. Docker acceptance now applies this migration and proves cross-run exposure/concurrency, daily actual-spend fencing, revocation, in-flight settlement, and workspace isolation; focused route/contract tests pass. These are accounting and review controls only: admin-entered pricing is an attestation, not independent verification, and provider dispatch remains disabled.

**Completed action: provider exception read model and inbox visibility.** Migration `20260924110000_platform_provider_exception_read_model.sql` records the triggering reservation/run/operation on new provider quota breaches and adds `platform_list_provider_exceptions(actor, workspace, after, after_id, limit)`. The service-only RPC rechecks active workspace membership, caps pages at 101 rows, merges UTC-day spend breaches with revoked adapter reviews, and exposes only safe IDs, timestamps and configured/observed amounts. The existing checkpoints read API adds the independent `provider_exceptions` cursor collection; cursors remain bound to workspace and collection. `PlatformInbox` presents read-only exception cards with links to the affected run where known. No acknowledgement clears evidence/fences and no retry control is exposed. Docker acceptance verifies actor enforcement, stable page continuation and bounds, linked run/reservation evidence, service-only RPC execution, and foreign-workspace denial; focused tests verify cursor workspace/collection fencing.

**Completed action: audited exception resolution and unknown-outcome recovery identity.** Migration `20260924120000_platform_exception_recovery_evidence.sql` adds append-only owner/admin resolutions keyed for safe replay; records provider/manual reconciliation evidence by bounded reference plus SHA-256 hash against the unchanged `unknown` effect receipt; and creates retry-intent rows only when evidence says `not_applied` and the exact pinned provider review is still active. Each intent has a new operation UUID and idempotency key while retaining the original operation, receipt and pinned provider review ID/hash. All writes append safe audit events once; no resolution mutates a quota breach, clears a UTC-day fence, transitions the original receipt, queues work or dispatches a provider. Authenticated POST routes are `provider-exceptions/[exceptionId]/resolve`, `effect-receipts/[receiptId]/recovery-review` and `effect-receipts/[receiptId]/retry-intents`; all require owner/admin access and same-origin JSON writes. Focused route tests and the disposable Postgres acceptance cover replay, role denial, evidence references/hashes, distinct retry identity/pin preservation, unchanged unknown receipt, revoked-review denial, immutable breach evidence and no service-role update/delete access to fences.

**Completed follow-up: concurrent retry-intent identity collision.** The initial retry-intent RPC handled concurrent replay on `(workspace_id, receipt_id, idempotency_key)` but left the separate `(workspace_id, retry_operation_id)` unique index exposed to a race: duplicate submissions could collide on the operation identity before the targeted `ON CONFLICT` handler ran. Forward migration `20260924150000_platform_retry_intent_idempotency_race.sql` serializes attempts on the receipt, explicitly returns an exact prior idempotent intent, rejects reuse of a key with a different operation, and converts an independently reused operation UUID into a deterministic unique-identity error. Acceptance now exercises exact concurrent replay and both conflicting identity combinations. `npm run test:db:concurrency` passed end-to-end in the disposable Docker/Postgres environment; temporary fixtures were removed. The new forward migration is included in the scheduler concurrency harness. No production database was changed and provider dispatch remains disabled.

**Completed action: recovery in the shared inbox.** Migration `20260924130000_platform_unknown_effect_inbox.sql` adds a member-authorized, service-only, bounded keyset read model for `unknown` effect receipts, with the latest reconciliation evidence, original provider pin and retry intent. The provider exception projection now includes its latest append-only resolution. The existing checkpoint API has independent workspace/collection-fenced cursors for unknown effects; the inbox offers owner/admin resolution, evidence capture and retry-intent controls, while read-only members can inspect evidence. Every surface states that intents are not dispatched; nothing enqueues work, mutates the original unknown receipt or removes a fence. The exact existing routes remain the only write path. Disposable Postgres acceptance proves page bounds, foreign-workspace denial, row joins, resolution visibility and authorization; focused tests prove cursor isolation.

**Completed action: workspace invitation and revocation lifecycle.** Migration `20260924140000_platform_workspace_invitations.sql` adds email-bound team invitations with SHA-256 token storage, seven-day application expiry (database accepts no more than 30 days), bounded roles (`owner` alone may grant `admin`), one pending invite per workspace/email, owner/admin list and invitation-revoke RPCs, signed-in-email acceptance, accepted-token replay for the same actor, and membership revocation. Owner memberships cannot be revoked; admins cannot revoke other admins. Every state mutation writes an audit event without logging the invite email/token. `workspaceInvitations.server.ts` returns a raw token once and marks delivery `not_sent`; no email integration is enabled. Workspace-first plus membership/invitation row locks serialize acceptance, revocation, and run access checks. Routes expose member/invitation list, creation, acceptance, and revocation; no direct table privileges are granted. Disposable Postgres acceptance verifies wrong-email denial, acceptance/replay, revoked-member access fencing, owner protection, disabled direct function access, and a concurrent protected operation holding its membership lock while revocation waits and then succeeds.

**Completed action: Jamie workspace read and confirm-before-launch proposals.** The workspace inbox now embeds Jamie chat with the selected workspace ID. `/api/jamie/vercel-chat` only enables private workspace tools after signed-in authentication and same-origin checks; each tool delegates to existing access-checked services. `read_workspace_runs` returns a bounded, filtered summary from `listRuns`. `propose_app_launch` calls `prepareAppLaunch` to validate the installed manifest, install revision, inputs and mapped resource revisions, then returns a transient proposal with a fresh idempotency key; it creates no run, checkpoint or stored proposal. The inbox renders the proposal and only a user click on **Start workflow** calls the existing `/apps/launch` API. No separate approval/checkpoint store was added, and no provider, email, publication or other external-effect tool is available. Focused Jamie unit tests pass; the production build/type check passes. Browser confirmation UX and an authenticated real-session acceptance remain pilot evidence, not claimed as complete.

**Completed action: two-app operational pilot rehearsal in disposable Postgres.** Extended `scripts/platform-followup-acceptance.mjs` to exercise the reviewed `real-estate-readiness` and `client-content-review` fixtures through `platform_start_app_run` and the existing `platform_run` scheduler contract. The owner installs both manifests (and configures the workspace budget in the existing follow-up setup); an active member starts the property-readiness run and supplies its requested intake; a separate reviewer is rejected when attempting to start work, then is attributed as the actor supplying human-review notes on a member-started content run. Both runs reach `completed`, preserve their exact responses, and emit the expected app-start audit events. Acceptance asserts there are no capability reservations or effect receipts for either run, consistent with both manifests declaring no capabilities. The worker event contract is enabled only inside this disposable acceptance fixture and is disabled again at the end. This is a database/role rehearsal with generated fixture identities—not evidence of a real signed-in browser session, real accepted business outputs, paid-provider accounting, or measured support time.

**Completed action: local authenticated browser acceptance for both app manifests.** `scripts/platform-local-auth-acceptance.mjs` applied `20260918090000_platform_app_launch_admission.sql` to the existing local Supabase stack after review (the additive migration is retained locally; no production migration was performed), created temporary local Supabase owner/member/reviewer identities with mock auth disabled, installed both inert manifests through the signed-in workspace API, and started both workflows through the authenticated launch route. The member recorded property intake; member install was denied; the reviewer could not launch, but could respond to the member-started content-review checkpoint. Both runs completed with exact response attribution and the property run pinned to its mapped source revision. Existing cancellation, cursor pagination, revoked/archived membership, foreign-user denial and real JWT RLS checks also passed. The harness removed temporary records/accounts and restored the workflow admission flag. The inbox GET on this older local stack lacked unrelated connector/provider read-model migrations, so the test exercises the run-detail/checkpoint route instead of coupling this app-launch acceptance to those extra read models. This proves local authentication and route wiring with generated local identities, not actual pilot participation or acceptance of business output.

| Pilot evidence | Disposable rehearsal result | Real pilot status |
| --- | --- | --- |
| Role boundaries | Disposable Postgres plus local-auth browser acceptance: owner install; admin quota update; member starts/responds; reviewer start denied and checkpoint response audited | Actual pilot participants still needed |
| Output and provenance | Two completed intake responses pinned to install hash/revision; property response also pins the mapped property revision | Not business-accepted output; collect user acceptance and content/source IDs |
| Economics | Zero capability reservations, estimated reserves, actual cost, and effect receipts | No paid calls; continue to keep providers disabled |
| Recovery / blocked actions | Role denials and revision fencing are asserted; recovery evidence remains covered by separate scheduler acceptance | Exercise recovery with actual pilot participants and log support minutes |
| Response/support time | Not represented by generated database timestamps | Measure elapsed human response and operator support time in the live pilot |

**Next action: collect real pilot evidence.** The local authenticated acceptance now passes. For the participant session, follow [the two-app pilot runbook](PLATFORM_TWO_APP_PILOT_RUNBOOK.md) in a dedicated non-production workspace with actual owner/member/reviewer accounts; record user-accepted output references, elapsed response time, blocked-action/recovery evidence and operator support minutes. Confirm reserved and consumed amounts remain zero while capability-empty manifests are in use. Do not fabricate credentials or claim generated local identities as real pilot accounts. Keep provider dispatch, paid calls, production migration and external effects disabled; a separate operational decision is required before live-provider activation. The spatial command-center proposal remains later Phase 3 UI over existing services, not a new scheduler/execution system. Conditions remain bounded predicates over persisted answers; do not add executable expressions or treat a checkpoint as a delivery receipt. Dates are target work slots, not permission to bypass a failed prerequisite.

Read this ledger, the active W-section and relevant source only. Do not load the archived 165-anchor specification by default. The Praxis and Keller / Westlake plans retain feature constraints and historical evidence; their older "next" sections do not override this queue. Keep one `keller-westlake` area, preserve source uncertainty, and keep email/publication authority separate from sprint approval.
