'use client';
import Link from 'next/link';
import { useEffect, useState } from 'react';

type Vibe = { vibeId: string; title?: string; name?: string; slug?: string; status?: string; description?: string; longDescription?: string; publishedRevisionId?: string; currentDraftVersion?: number; updatedAt?: string; draftPayload?: any };

export function VibeEditor({ vibeId }: { vibeId: string }) {
  const [vibe, setVibe] = useState<Vibe | null>(null);
  const [error, setError] = useState('');
  const [saving, setSaving] = useState(false);
  useEffect(() => { fetch(`/api/admin/vibes/${encodeURIComponent(vibeId)}`).then(async (response) => { if (!response.ok) throw new Error('Unable to load vibe.'); return response.json(); }).then((payload) => setVibe(payload.vibe)).catch((reason) => setError(reason.message)); }, [vibeId]);
  if (error) return <main className="min-h-screen bg-slate-100 p-8"><p role="alert" className="text-red-700">{error}</p></main>;
  if (!vibe) return <main className="min-h-screen bg-slate-100 p-8 text-slate-500">Loading vibe…</main>;
  const draft = vibe.draftPayload || { title: vibe.title || vibe.name || '', slug: vibe.slug || vibe.vibeId, description: vibe.longDescription || vibe.description || '', tokens: { visual: { theme: { colors: { primary: '#2563eb', background: '#0f172a', surface: '#1e293b', textPrimary: '#f8fafc', textSecondary: '#cbd5e1' }, typography: { fontFamilyHeading: 'Inter', fontFamilyBody: 'Inter', baseFontSize: '16px' }, layout: {} }, effects: {} }, linguistic: { voice: { primaryTone: 'warm' }, vocabulary: {}, systemDirectives: ['Be clear, useful, and grounded.'] } }, source: { kind: 'manual' } };
  const saveDraft = async (form: HTMLFormElement) => { setSaving(true); const data = new FormData(form); const next = { ...draft, title: String(data.get('title')), slug: String(data.get('slug')), description: String(data.get('description')), tokens: { ...draft.tokens, visual: { ...draft.tokens.visual, theme: { ...draft.tokens.visual.theme, colors: { ...draft.tokens.visual.theme.colors, primary: String(data.get('primary')), background: String(data.get('background')), surface: String(data.get('surface')), textPrimary: String(data.get('textPrimary')), textSecondary: String(data.get('textSecondary')) } }, }, linguistic: { ...draft.tokens.linguistic, voice: { ...draft.tokens.linguistic.voice, primaryTone: String(data.get('primaryTone')) } } } }; const response = await fetch(`/api/admin/vibes/${encodeURIComponent(vibeId)}?tenantId=default`, { method: 'PATCH', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ draft: next, expectedVersion: vibe.currentDraftVersion ?? 0 }) }); if (!response.ok) setError('Draft could not be saved. It may have changed in another session.'); else setVibe((await response.json()).vibe); setSaving(false); };
  return <main className="min-h-screen bg-slate-100 px-4 py-8 text-slate-900 sm:px-8"><div className="mx-auto max-w-6xl"><form onSubmit={(event) => { event.preventDefault(); void saveDraft(event.currentTarget); }}><div className="mb-6 flex flex-wrap items-start justify-between gap-4"><div><Link href="/admin/vibes" className="text-sm font-semibold text-slate-500 hover:underline">← All vibes</Link><h1 className="mt-3 text-3xl font-black">Edit vibe</h1><p className="mt-1 text-sm text-slate-500">/{vibe.slug || vibe.vibeId}</p></div><div className="flex gap-2"><Link href={`/admin/vibes/${vibeId}/revisions`} className="rounded-lg border border-slate-300 bg-white px-3 py-2 text-sm font-bold">Revision history</Link><button className="rounded-lg bg-slate-900 px-3 py-2 text-sm font-bold text-white" disabled={saving}>{saving ? 'Saving…' : 'Save draft'}</button></div></div><div className="grid gap-5 lg:grid-cols-[1fr_320px]"><section className="space-y-5"><article className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="text-sm font-black uppercase tracking-wide text-slate-500">Metadata</h2><div className="mt-4 grid gap-4 sm:grid-cols-2"><label className="text-xs font-bold uppercase text-slate-500">Title<input name="title" defaultValue={draft.title} className="mt-2 w-full rounded-md border border-slate-300 px-3 py-2 text-sm font-normal normal-case" /></label><label className="text-xs font-bold uppercase text-slate-500">Slug<input name="slug" defaultValue={draft.slug} className="mt-2 w-full rounded-md border border-slate-300 px-3 py-2 font-mono text-sm font-normal normal-case" /></label><label className="text-xs font-bold uppercase text-slate-500 sm:col-span-2">Description<textarea name="description" defaultValue={draft.description} className="mt-2 min-h-28 w-full rounded-md border border-slate-300 px-3 py-2 text-sm font-normal normal-case" /></label></div></article><article className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="text-sm font-black uppercase tracking-wide text-slate-500">Visual theme</h2><div className="mt-4 grid gap-4 sm:grid-cols-2">{(['primary', 'background', 'surface', 'textPrimary', 'textSecondary'] as const).map((key) => <label key={key} className="text-xs font-bold uppercase text-slate-500">{key}<input name={key} type="color" defaultValue={draft.tokens.visual.theme.colors[key]} className="mt-2 block h-10 w-full rounded border border-slate-300 bg-white p-1" /></label>)}</div></article><article className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="text-sm font-black uppercase tracking-wide text-slate-500">Jamie voice</h2><label className="mt-4 block text-xs font-bold uppercase text-slate-500">Primary tone<select name="primaryTone" defaultValue={draft.tokens.linguistic.voice.primaryTone} className="mt-2 w-full rounded-md border border-slate-300 px-3 py-2 text-sm font-normal normal-case"><option>warm</option><option>concise</option><option>analytical</option><option>energetic</option><option>tactical</option><option>luxurious</option><option>playful</option></select></label></article></section><aside className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="text-sm font-black uppercase tracking-wide text-slate-500">Editorial status</h2><p className="mt-3 inline-flex rounded-full bg-slate-100 px-3 py-1 text-xs font-bold">{vibe.status || 'draft'}</p>{error && <p role="alert" className="mt-4 text-sm text-red-700">{error}</p>}<dl className="mt-5 space-y-3 text-sm"><div className="flex justify-between gap-3"><dt className="text-slate-500">Draft version</dt><dd className="font-semibold">{vibe.currentDraftVersion ?? 0}</dd></div><div className="flex justify-between gap-3"><dt className="text-slate-500">Published revision</dt><dd className="font-semibold">{vibe.publishedRevisionId ? 'Assigned' : 'None'}</dd></div></dl></aside></div></form></div></main>;
}
