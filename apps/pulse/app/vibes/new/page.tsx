'use client';
import { useState } from 'react'; import { useRouter } from 'next/navigation'; import Link from 'next/link';
function toSlug(value: string) { return value.toLowerCase().trim().replace(/[^a-z0-9]+/g, '-').replace(/^-+|-+$/g, ''); }
export default function NewVibePage() {
  const router = useRouter(); const [title, setTitle] = useState(''); const [slug, setSlug] = useState(''); const [slugEdited, setSlugEdited] = useState(false); const [description, setDescription] = useState(''); const [preset, setPreset] = useState(''); const [error, setError] = useState(''); const [saving, setSaving] = useState(false);
  async function create() { setSaving(true); setError(''); const response = await fetch('/api/vibes', { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ title, slug, description, ...(preset ? { preset } : {}) }) }); const payload = await response.json(); if (!response.ok) { setError(payload.error || 'Unable to create vibe.'); setSaving(false); return; } router.push(`/vibes/${payload.vibe.vibeId}/edit`); }
  return <main className="min-h-screen bg-slate-100 px-4 py-8 text-slate-900 sm:px-8"><div className="mx-auto max-w-2xl"><Link href="/vibes" className="text-sm font-semibold text-slate-500 hover:underline">← All Vibes</Link><h1 className="mt-4 text-3xl font-black">Add New Vibe</h1><p className="mt-1 text-sm text-slate-600">Create a draft identity to continue in the structured editor.</p><form className="mt-6 space-y-5 rounded-xl border border-slate-200 bg-white p-6 shadow-sm" onSubmit={(event) => { event.preventDefault(); void create(); }}><label className="block text-sm font-bold">Starting style<select value={preset} onChange={(event) => setPreset(event.target.value)} className="mt-2 w-full rounded-md border border-slate-300 px-3 py-2 font-normal"><option value="">Default</option><option value="editorial">Editorial warmth</option><option value="market-intelligence">Market intelligence</option></select><span className="mt-2 block text-xs font-normal text-slate-500">Copied into this new draft only; you can edit every value afterward.</span></label><label className="block text-sm font-bold">Title<input required value={title} onChange={(event) => { const next = event.target.value; setTitle(next); if (!slugEdited) setSlug(toSlug(next)); }} className="mt-2 w-full rounded-md border border-slate-300 px-3 py-2 font-normal" /></label><label className="block text-sm font-bold">Slug<input required pattern="[a-z0-9]+(?:-[a-z0-9]+)*" value={slug} onChange={(event) => { setSlug(event.target.value); setSlugEdited(event.target.value.length > 0); }} className="mt-2 w-full rounded-md border border-slate-300 px-3 py-2 font-mono font-normal" /></label><label className="block text-sm font-bold">Description<textarea value={description} onChange={(event) => setDescription(event.target.value)} className="mt-2 min-h-32 w-full rounded-md border border-slate-300 px-3 py-2 font-normal" /></label>{error ? <p role="alert" className="text-sm text-red-700">{error}</p> : null}<button disabled={saving} className="rounded-lg bg-slate-900 px-4 py-2 text-sm font-bold text-white disabled:opacity-50">{saving ? 'Creating…' : 'Create Draft'}</button></form></div></main>;
}
