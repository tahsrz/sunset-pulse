'use client';
type Item={id:string;title:string;priority:number;status:string}; type Sprint={id:string;name:string;goal:string;status:string;revision?:number};
export function SprintCard({sprint,items,onApprove,onRemove}:{sprint:Sprint;items:Item[];onApprove:()=>void;onRemove:(id:string,sprintId:string,revision:number)=>void}){return <article className="mt-2 rounded-xl bg-slate-950/60 p-4"><div className="flex justify-between"><b>{sprint.name}</b><span>{sprint.status}</span></div><p className="mt-2 text-sm">{sprint.goal}</p><div className="mt-3 grid gap-1 text-xs text-slate-400">{items.filter((item)=>item.status!=='cancelled').map((item)=><div key={item.id} className="flex justify-between"><span>P{item.priority} · {item.title}</span>{sprint.status==='proposed'&&<button onClick={()=>onRemove(item.id,sprint.id,sprint.revision ?? 1)} className="text-red-200">Remove</button>}</div>)}</div>{sprint.status==='proposed'&&<button onClick={onApprove} className="mt-2 text-emerald-200">Approve sprint</button>}</article>}
