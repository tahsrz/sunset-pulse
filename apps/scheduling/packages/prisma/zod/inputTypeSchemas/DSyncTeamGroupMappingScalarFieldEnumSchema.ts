import { z } from 'zod';

export const DSyncTeamGroupMappingScalarFieldEnumSchema = z.enum(['id','organizationId','teamId','directoryId','groupName']);

export default DSyncTeamGroupMappingScalarFieldEnumSchema;
