import { z } from 'zod';

export const AttributeSyncFieldMappingScalarFieldEnumSchema = z.enum(['id','integrationFieldName','attributeId','enabled','integrationAttributeSyncId','createdAt','updatedAt']);

export default AttributeSyncFieldMappingScalarFieldEnumSchema;
